---
name: ingest
description: 워크스페이스에 새 정보가 들어올 때 사용한다. 사용자가 새 사실, 의견, 예측, 결정을 알리거나, _reference/에 자료가 dump되었거나, 웹 검색이나 MCP 등 외부 도구에서 사실을 가져왔을 때 트리거된다.
---

# 새 정보 ingest

이 스킬은 절차를 로드하는 포인터다. 절차 본체(SSOT(single source of truth))는 [AGENTS/ingest-workflow.md](../../../AGENTS/ingest-workflow.md).

1. 위 SSOT 문서를 지금 읽는다. 기억으로 단언하지 않는다.
2. "표준 시퀀스" 각 단계를 순서대로 적용한다. 건너뛰지 않는다.
3. `_reference/` 자료라면 같은 문서의 "`_reference/` 폴더 처리" 절을 적용한다.
